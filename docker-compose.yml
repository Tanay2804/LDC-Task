services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: jenkins-nextjs-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
